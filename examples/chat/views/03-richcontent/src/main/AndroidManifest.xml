<?xml version="1.0" encoding="utf-8"?>
<!-- Copyright PolyAI Limited -->
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Agent avatars + attachment images load over the network. -->
    <uses-permission android:name="android.permission.INTERNET" />
    <!-- Foreground new-message notifier posts a local notification (API 33+ requires this). -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <application
        android:name=".RichContentApplication"
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.PolyRichContent">
        <activity android:name=".ChatActivity" android:exported="true" android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
