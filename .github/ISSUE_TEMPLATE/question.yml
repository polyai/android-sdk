name: Question
description: Ask about using the SDK.
title: "[Question]: "
labels: [question]
body:
  - type: textarea
    id: question
    attributes:
      label: Your question
      description: Check the README and the `examples/` apps first.
    validations:
      required: true
  - type: textarea
    id: context
    attributes:
      label: Context
